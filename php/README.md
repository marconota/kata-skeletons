# Kata Skeleton PHP

Empty PHP project with a failing test.

For install dependencies just run:

```
$ composer install

```

To execute the tests just run:

```
$ vendor/bin/phpunit
```

You need PHP 7.4 or above.

